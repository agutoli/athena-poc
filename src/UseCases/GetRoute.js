// const dependencies = {
//   Routes: require('../routes')
// }
//
// const defaultValue = {
//   index: null,
//   types: [],
//   func: null
// }

module.exports = (path, injection) => {
  // const { Routes } = Object.assign({}, dependencies, injection)
  // return Routes.reduce((stored, currentRoute) => {
  //   let value = currentRoute.match(path)
  //   if (!value) return stored
  //
  //   if (value.types) value.types = value.types.split(',')
  //
  //   return value
  // }, defaultValue)
}
